#include <stdio.h>

int main() 
{
	int i = ;
	int user;
	
	while (user > 0)
	{
		printf("Enter a bound: ");
		scanf("%d", &user);
		
		while (i <= user)
		{
			printf("%d\n", 1);
			i+=;
		}
	}
	
	return 0;
}
